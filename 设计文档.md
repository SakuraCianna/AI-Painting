# 纯语音控制绘图工具设计文档

> 日期: 2026-06-12
> 分支: codex/AI-Painting
> 实现阶段: 第一阶段 MVP

## 1. 设计目标

第一阶段目标是打通“语音文本输入 -> 中文指令解析 -> 绘图操作执行 -> SVG 画布渲染 -> SQLite 持久化 -> 撤销恢复”的最小闭环。

本阶段不接入付费 ASR、LLM、账号体系和云存储, 避免把核心绘图闭环绑定到外部服务。

## 2. 计划支持的指令能力

### 2.1 画布能力

- 新建画布
- 设置横向或竖向尺寸
- 设置背景颜色
- 保存作品版本
- 导出 PNG
- 清空画布前二次确认

### 2.2 绘图能力

- 圆形
- 矩形
- 椭圆
- 三角形
- 线条
- 箭头
- 星形
- 文本
- 常见颜色词
- 常见位置词
- 常见尺寸参数

### 2.3 编辑能力

- 选择最近对象
- 修改最近对象颜色
- 加粗最近对象边框
- 移动最近对象
- 撤销
- 恢复

### 2.4 复杂指令能力

- 把一句复杂命令拆解为多个可执行操作
- 每个操作写入操作历史
- 操作失败时停止并返回错误

## 3. 本阶段已实现的指令能力

### 3.1 已实现画布指令

- “新建一张横向白色画布”
- “新建一张竖向画布”
- “保存作品”
- “保存作品, 名字叫夏日练习”
- “导出 PNG”
- “清空画布”返回确认问题, 当前不直接执行

### 3.2 已实现绘图指令

- “画一个蓝色圆形在中间, 半径一百”
- “画一个矩形”
- “画一个椭圆”
- “画一个三角形”
- “画一条线”
- “画一个箭头”
- “画一个黄色星星在左边”
- “写文字是你好”

### 3.3 已实现编辑指令

- “把它改成浅蓝色”
- “加粗边框”
- “向右移动二十像素”
- “往上移动一点”
- “撤销”
- “退回一步”
- “恢复”

### 3.4 已实现复杂指令

- “画一个房子, 红色屋顶, 蓝色门, 两扇窗户”
- “画三颗黄色星星, 从左到右变小”
- “把所有蓝色图形改成绿色, 然后整体向上移动一点”

房子指令会拆成五个绘图操作:

1. 房子主体
2. 红色屋顶
3. 蓝色门
4. 窗户1
5. 窗户2

星星指令会按数量生成多个星形对象, 并根据“从左到右变小”调整位置和半径。

批量编辑指令会先筛选所有蓝色对象改成绿色, 再把整体对象向上移动默认 20 像素。

### 3.5 已实现性能优化

- 为 `drawing_objects`、`operations`、`voice_command_logs` 增加常用查询索引
- 多步骤计划只清理一次 redo 栈
- 多步骤计划成功后统一提交 SQLite 事务
- 多步骤计划失败时回滚整次计划
- 撤销查询增加 `rowid DESC`, 保证同一时间内写入的计划步骤按最新写入顺序撤销

### 3.6 已实现前端反馈增强

- 展示最近一次执行计划的步骤数量
- 展示解析置信度
- 把后端操作类型转换为中文步骤标签
- 使用浏览器语音合成播报执行结果

### 3.7 已实现前端视觉精修

- 将工作台视觉方向调整为 Google Workspace 和 Material 3 风格
- 使用浅色 surface、蓝色 primary、低噪声边框和轻量 elevation
- 去掉偏营销化和 AI 产品化的视觉表达, 改为更像生产力工具的布局
- 将侧栏整理为状态、识别文本、执行计划、操作历史四个清晰区域
- 增加 React StrictMode 开发态防重保护, 避免重复创建初始作品

## 4. 暂未完成能力与原因

- 后端 ASR: 本阶段使用浏览器 Web Speech API 做原型验证, 后端 ASR 需要额外模型或第三方服务选型
- 多对象语义选择: 当前支持按颜色批量编辑, 但“选中所有蓝色圆形并命名”这类跨条件选择仍需更完整的对象查询和确认交互
- 图层编组: 需要新增 group 数据结构和批量操作撤销策略
- 旋转对象: 当前几何模型尚未统一 transform 字段
- 渐变和阴影: 第一阶段先保证基础矢量样式稳定
- SVG 后端导出: 当前 PNG 导出由前端基于 SVG 转换完成, 后端导出需要额外渲染依赖
- 方言和中英混合: 需要更强的 ASR 和 NLU 评测集
- 用户自定义词表: 需要用户配置持久化和冲突处理

## 5. 语音识别方案

前端 `useVoiceRecognition` 使用浏览器 `SpeechRecognition` 或 `webkitSpeechRecognition`:

- 语言设置为 `zh-CN`
- 开启连续识别
- 开启临时识别结果
- 最终识别文本提交给后端
- 识别结束后自动尝试恢复监听

已知限制:

- 不同浏览器支持程度不同
- 浏览器可能要求麦克风权限或用户激活
- 浏览器语音识别可能受网络、系统语言和麦克风质量影响

## 6. 指令解析架构

后端 `command_parser.py` 采用规则解析:

1. 文本规范化
2. 同义词替换
3. 中文数字解析
4. 颜色词映射
5. 形状词映射
6. 方位词映射
7. 意图判断
8. 操作计划生成

规则解析的优点是延迟低、可测试、可解释。缺点是泛化能力有限, 后续可以在规则解析前增加 ASR 纠错, 或在规则解析失败时接入 LLM 规划。

## 7. 复杂指令拆解策略

复杂指令返回 `CommandPlan`, 其中包含多个 `OperationRequest`。前端只关心最终画布和执行消息, 后端负责逐步执行和持久化。

当前策略:

- 低风险复杂指令直接执行
- 清空画布这类高风险指令只返回确认问题
- 每个状态变更操作写入 `operations`
- 失败时抛出错误, 不继续执行后续操作
- 多步骤计划在同一执行单元中提交, 失败时回滚本次计划

## 8. 延迟优化策略

当前策略:

- 简单规则解析在后端同步完成
- SQLite 写入只保存必要 JSON
- SQLite 增加按作品、类型、状态和创建时间的查询索引
- 多步骤计划减少重复 redo 栈清理和中间提交
- 前端 SVG 增量来自后端返回的最新对象列表
- 前端初始化作品时增加防重保护, 减少开发态重复 API 调用
- 语音识别、解析、执行耗时写入 `voice_command_logs.latency_json`

后续可优化:

- WebSocket 推送复杂计划执行进度
- 前端先乐观渲染低风险操作
- 更细粒度的对象增量更新

## 9. SQLite 表结构

当前后端初始化以下表:

- `artworks`: 作品基础信息
- `artwork_versions`: 作品版本快照
- `drawing_objects`: 当前绘图对象
- `operations`: 可撤销操作历史
- `voice_command_logs`: 语音指令解析和执行日志

SQLite 连接层会执行 `PRAGMA foreign_keys = ON`, 保证外键约束生效。

当前索引:

- `idx_drawing_objects_artwork_z`
- `idx_drawing_objects_artwork_type`
- `idx_operations_artwork_status_created`
- `idx_voice_logs_artwork_created`

## 10. API 设计

已实现 API:

- `GET /health`
- `POST /api/artworks`
- `GET /api/artworks`
- `GET /api/artworks/{artwork_id}`
- `POST /api/commands/parse`
- `POST /api/artworks/{artwork_id}/commands`
- `POST /api/artworks/{artwork_id}/undo`
- `POST /api/artworks/{artwork_id}/redo`

## 11. 前端交互设计

前端第一屏就是绘图工作台, 包含:

- 顶部作品标题和状态
- 中央 SVG 画布
- 右侧监听状态
- 识别文本
- 操作历史

前端没有绘图工具栏、拖拽交互或键盘快捷绘图路径。

## 12. 测试设计

后端测试覆盖:

- 中文数字解析
- 新建画布指令解析
- 圆形颜色和半径解析
- 房子复杂指令拆解
- 撤销同义词
- 创建作品 API
- 语音文本执行为绘图对象
- 撤销和恢复 API
- 批量绘制三颗星星
- 批量改色、整体移动和分步撤销
- 最近对象缩放

前端当前通过 TypeScript 构建验证基础类型和打包流程。

## 13. 当前验证结果

已验证:

- 后端 pytest 已通过, 覆盖解析器、API、SQLite 持久化、撤销和恢复
- 前端 Vite build 已通过, 覆盖 TypeScript 编译和生产打包
- 本地后端服务已启动并验证 `GET /health` 返回 `ok`
- 本地前端服务已启动并验证首页 HTTP 状态为 `200`

未验证:

- 浏览器真实麦克风识别是否可用, 因为该能力依赖浏览器权限、系统麦克风和具体浏览器实现
- PNG 下载是否被浏览器策略阻止, 因为需要真实浏览器交互环境确认下载策略

## 14. 后续建议

1. 增加命令评测集, 覆盖 50 到 100 条中文语音指令。
2. 引入后端 ASR 适配层, 保留浏览器识别作为轻量模式。
3. 增加对象命名和多对象选择能力。
4. 增加 WebSocket 进度推送。
5. 增加 Playwright 浏览器验证, 检查画布渲染和导出路径。
