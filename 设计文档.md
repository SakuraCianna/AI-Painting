# 纯语音控制绘图工具设计文档

> 日期: 2026-06-12
> 分支: codex/AI-Painting
> 实现阶段: MVP 后产品化扩展

长期产品目标和商业级演进路线见 [ROADMAP.md](ROADMAP.md)。

## 1. 设计目标

第一阶段目标是打通“语音文本输入 -> 中文指令解析 -> 绘图操作执行 -> SVG 画布渲染 -> SQLite 持久化 -> 撤销恢复”的最小闭环。

当前阶段采用可降级外部能力: 小米 MiMo ASR 和 MiMo-v2.5-Pro 用于提升识别与复杂指令规划稳定性, 本地规则解析、本地 ASR 适配和 Web Speech API 作为降级路径, 避免外部服务不可用时完全阻断核心绘图闭环。

## 2. 计划支持的指令能力

### 2.1 画布能力

- 新建画布
- 设置横向或竖向尺寸
- 设置背景颜色
- 保存作品版本
- 导出 PNG
- 清空画布前二次确认

### 2.2 绘图能力

- 圆形
- 矩形
- 椭圆
- 三角形
- 线条
- 箭头
- 星形
- 文本
- 多边形
- 路径
- 贝塞尔曲线
- 常见颜色词
- 常见位置词
- 常见尺寸参数

### 2.3 编辑能力

- 选择最近对象
- 修改最近对象颜色
- 加粗最近对象边框
- 移动最近对象
- 撤销
- 恢复

### 2.4 复杂指令能力

- 把一句复杂命令拆解为多个可执行操作
- 每个操作写入操作历史
- 操作失败时停止并返回错误

## 3. 本阶段已实现的指令能力

### 3.1 已实现画布指令

- “新建一张横向白色画布”
- “新建一张竖向画布”
- “保存作品”
- “保存作品, 名字叫夏日练习”
- “导出 PNG”
- “清空画布”返回确认问题, “确认清空”后执行清空, “取消清空”会取消待确认操作

### 3.2 已实现绘图指令

- “画一个蓝色圆形在中间, 半径一百”
- “画一个矩形”
- “画一个椭圆”
- “画一个三角形”
- “画一条线”
- “画一个箭头”
- “画一个黄色星星在左边”
- “写文字是你好”

### 3.3 已实现编辑指令

- “把它改成浅蓝色”
- “加粗边框”
- “向右移动二十像素”
- “往上移动一点”
- “撤销”
- “退回一步”
- “恢复”

### 3.4 已实现复杂指令

- “画一个房子, 红色屋顶, 蓝色门, 两扇窗户”
- “画三颗黄色星星, 从左到右变小”
- “把所有蓝色图形改成绿色, 然后整体向上移动一点”

房子指令会拆成五个绘图操作:

1. 房子主体
2. 红色屋顶
3. 蓝色门
4. 窗户1
5. 窗户2

星星指令会按数量生成多个星形对象, 并根据“从左到右变小”调整位置和半径。

批量编辑指令会先筛选所有蓝色对象改成绿色, 再把整体对象向上移动默认 20 像素。

### 3.5 已实现性能优化

- 为 `drawing_objects`、`operations`、`voice_command_logs` 增加常用查询索引
- 多步骤计划只清理一次 redo 栈
- 多步骤计划成功后统一提交 SQLite 事务
- 多步骤计划失败时回滚整次计划
- 撤销查询增加 `rowid DESC`, 保证同一时间内写入的计划步骤按最新写入顺序撤销

### 3.6 已实现前端反馈增强

- 展示最近一次执行计划的步骤数量
- 展示解析置信度
- 展示计划来源, 区分规则解析、MiMo 规划和规则兜底
- 展示计划解释文本和 `ScenePlan` 拆解步骤
- 把后端操作类型转换为中文步骤标签
- 使用小米 MiMo TTS 播报执行结果

### 3.7 已实现前端视觉精修

- 将工作台视觉方向调整为 Google Workspace 和 Material 3 风格
- 使用浅色 surface、蓝色 primary、低噪声边框和轻量 elevation
- 去掉偏营销化和 AI 产品化的视觉表达, 改为更像生产力工具的布局
- 将侧栏整理为状态、识别文本、执行计划、操作历史四个清晰区域
- 页面锁定在视口高度内, 禁止页面级上下滚动
- 右侧控制台压缩宽度和卡片密度, 历史区域内部滚动
- 左侧移除作品标题栏, 只保留全高度画布
- 四个 icon 工具按钮移动到“控制台”标题右侧
- 增加 React StrictMode 开发态防重保护, 避免重复创建初始作品
- 使用 Iconify 的 Material Symbols 本地图标数据包构建 icon-only 工具栏
- 工具栏按钮通过 `title`、`aria-label` 和 hover tooltip 暴露功能说明
- icon 按钮只承担监听控制和导出, 不增加鼠标绘图路径

### 3.8 已实现语音绘图内核 2.0 起步能力

- 绘图对象增加 `layer_id`、`group_id`、`semantic_tags` 和 `transform` 元数据
- 数据库启动时会为旧版 `drawing_objects` 表补齐新增元数据列
- 房子复杂指令会为主体、屋顶、门和窗户写入 `house.*` 语义标签
- 支持“画一个黄色圆形, 命名为太阳, 放到前景层”
- 支持“把它命名为太阳”
- 支持“把前景层所有对象向右移动一点”
- 支持“把房子的窗户都变大”
- 支持 `ScenePlan`、`ScenePlanStep` schema 骨架, 用于后续复杂场景解释和计划展示

### 3.9 已实现路径图形与复杂指令评测集

- 新增 `polygon`、`path` 和 `bezier` 绘图对象类型
- 支持“画一个绿色五边形在左边”
- 支持“画一条弯曲小路”
- 支持“画一条棕色贝塞尔曲线在中间”
- 支持“画一个太阳然后在下面加一片云”这类两步天空元素规则计划
- 路径对象支持语音移动和缩放, 撤销恢复继续走统一操作历史
- 前端 SVG 画布支持结构化路径命令渲染
- 新增 `docs/evaluation/complex_voice_commands.json` 复杂语音指令评测集
- 评测集区分 `rules` 和 `planner_expected`, 避免把尚需 LLM 规划的能力写成已完成

### 3.10 已实现低置信度追问与计划解释

- `CommandPlan` 增加 `explanation` 和 `planner_source`
- 后端会为规则解析、MiMo 规划和规则兜底统一补充计划解释
- “画一个温馨的小屋, 左边有两棵树, 右边有一条弯曲小路, 天空有三朵云”这类多主体场景会先返回追问
- “把画面改成夜晚, 保留房子的形状, 给窗户加暖黄色灯光”这类全局改造指令会先进入确认路径
- 规则层不会把 `planner_expected` 用例误执行成一个简单对象
- 前端执行计划卡片会展示置信度、计划来源、摘要、拆解步骤和确认状态

### 3.11 已实现清空画布确认链

- 首次说“清空画布”时, 后端返回 `requires_confirmation=true` 和高风险提示, 不删除任何对象
- 待确认计划写入 `voice_command_logs`, 状态为 `needs_confirmation`
- 继续说“确认清空”时, 后端读取同一作品最近的待确认 `clear_canvas` 计划并执行
- 清空操作作为一条 `operations` 历史记录写入, 逆操作保存被清空对象快照
- “撤销”会恢复被清空的对象, “恢复”会再次清空
- “取消清空”会把待确认日志标记为 `canceled`, 不执行绘图变更

## 4. 暂未完成能力与原因

- ASR 真机评测: 已接入小米 MiMo ASR 和本地 ASR 适配层, 但仍需要真实账号、真实麦克风和方言噪声样本做稳定性评测
- 多对象语义选择: 已支持按颜色、图层和语义标签筛选的第一版, 但“选中所有蓝色圆形并命名”这类更完整的组合查询和确认交互仍需继续扩展
- 图层编组: 已有 `group_id` 和 `layer_id` 元数据, 但还没有完整的图层面板、组内排序和组级变换策略
- 旋转对象: 当前几何模型尚未统一 transform 字段
- 渐变和阴影: 第一阶段先保证基础矢量样式稳定
- 路径自由编辑: 已能通过语音创建和移动路径对象, 但还没有逐点调整、路径布尔运算和语音控制曲率
- 完整复杂场景自动生成: 已能识别并追问多主体场景, 但真实自动拆解仍依赖 MiMo 规划器和后续评测
- SVG 后端导出: 当前 PNG 导出由前端基于 SVG 转换完成, 后端导出需要额外渲染依赖
- 方言和中英混合: 需要更强的 ASR 和 NLU 评测集
- 用户自定义词表: 需要用户配置持久化和冲突处理

## 5. 语音识别方案

当前采用“后端 ASR 优先, 浏览器识别兜底”的分层方案:

1. 前端通过 Web Audio API 采集麦克风音频。
2. 前端用简单能量阈值和静音窗口切分单条语音指令。
3. 前端将音频编码为 `audio/wav` data URL, 提交到后端 `/api/asr/transcribe`。
4. 后端按 `AI_PAINTING_ASR_PROVIDERS` 顺序尝试 Provider, 默认顺序为 `xiaomi,local`。
5. 小米 Provider 调用 `mimo-v2.5-asr`, 音频通过 `input_audio.data` 字段传入, 语种默认 `zh`。
6. 本地 Provider 支持本地 HTTP 服务或本地命令模板, 作为小米 ASR 后的第一备用方案。
7. 如果后端 ASR 不可用, 前端降级为浏览器 `SpeechRecognition` 或 `webkitSpeechRecognition`。

已实现能力:

- 小米 MiMo ASR 非流式调用适配
- 本地 ASR HTTP 服务适配
- 本地 ASR 命令模板适配
- 小米 ASR 请求失败时支持短暂重试
- 后端 Provider 状态查询
- 后端 Provider 失败后返回可解释的尝试记录
- 前端录音、静音切分、WAV 编码和后端转写调用
- 语音识别改为用户点击麦克风后才开始, 页面加载不自动打开麦克风
- 前端录音触发阈值提高, 降低轻微环境声被当作指令的概率
- 执行结果使用小米 MiMo TTS 播报, 不再使用浏览器 `speechSynthesis` 作为播报方案
- Web Speech API 最终兜底

已知限制:

- 当前小米 ASR 接口按整段音频提交, 不是真正的低延迟 WebSocket 流式识别
- 前端静音切分使用能量阈值, 噪声较大时可能提前或延后截断
- 本地 ASR 只提供适配入口, 不随项目下载大模型权重
- Web Speech API 仍受浏览器支持、系统权限、网络和系统语言影响
- 尚未用真实小米 API Key、真实麦克风和方言噪声样本做端到端评测
- 小米 TTS 需要网络和 `MIMO_API_KEY`, 真实播放效果仍需在浏览器中验收

## 6. 指令解析架构

后端采用“规则解析优先, MiMo 规划兜底”的混合方案:

1. 文本规范化
2. 同义词替换
3. 中文数字解析
4. 颜色词映射
5. 形状词映射
6. 方位词映射
7. 意图判断
8. 操作计划生成
9. 低置信度或复杂连接词场景调用小米 MiMo-v2.5-Pro 生成受控 JSON 计划
10. 后端校验操作类型和对象类型, 通过后才交给绘图引擎执行
11. 规则兜底为多主体场景返回追问和解释, 防止误执行半成品计划

规则解析的优点是延迟低、可测试、可解释。MiMo 规划器用于补足复杂自然语言拆解能力, 但只作为兜底方案, 避免所有指令都依赖外部模型造成延迟和成本上升。

## 7. 复杂指令拆解策略

复杂指令返回 `CommandPlan`, 其中包含多个 `OperationRequest`。前端只关心最终画布和执行消息, 后端负责逐步执行和持久化。

当前策略:

- 低风险复杂指令直接执行
- 清空画布这类高风险指令先返回确认问题, 只有收到同一作品后续确认语音才执行
- 多主体场景和全局改造指令在规则层先返回追问
- 每个状态变更操作写入 `operations`
- 失败时抛出错误, 不继续执行后续操作
- MiMo 规划失败时回退到规则解析结果, 不影响基础指令
- MiMo 规划最多允许有限步骤, 防止一次语音生成过大的执行计划
- 多步骤计划在同一执行单元中提交, 失败时回滚本次计划

## 8. 延迟优化策略

当前策略:

- 简单规则解析在后端同步完成
- SQLite 写入只保存必要 JSON
- SQLite 增加按作品、类型、状态和创建时间的查询索引
- 多步骤计划减少重复 redo 栈清理和中间提交
- 前端 SVG 增量来自后端返回的最新对象列表
- 前端初始化作品时增加防重保护, 减少开发态重复 API 调用
- 语音识别、解析、执行耗时写入 `voice_command_logs.latency_json`

后续可优化:

- WebSocket 推送复杂计划执行进度
- 前端先乐观渲染低风险操作
- 更细粒度的对象增量更新

## 9. SQLite 表结构

当前后端初始化以下表:

- `artworks`: 作品基础信息
- `artwork_versions`: 作品版本快照
- `drawing_objects`: 当前绘图对象
- `operations`: 可撤销操作历史
- `voice_command_logs`: 语音指令解析和执行日志

SQLite 连接层会执行 `PRAGMA foreign_keys = ON`, 保证外键约束生效。

当前索引:

- `idx_drawing_objects_artwork_z`
- `idx_drawing_objects_artwork_type`
- `idx_drawing_objects_artwork_layer`
- `idx_drawing_objects_artwork_group`
- `idx_operations_artwork_status_created`
- `idx_voice_logs_artwork_created`

`drawing_objects` 元数据字段:

- `layer_id`: 对象所在图层, 默认 `base`
- `group_id`: 对象所属语义组
- `semantic_tags_json`: 对象语义标签列表
- `transform_json`: 为旋转、缩放、倾斜等后续变换预留

## 10. API 设计

已实现 API:

- `GET /health`
- `POST /api/artworks`
- `GET /api/artworks`
- `GET /api/artworks/{artwork_id}`
- `POST /api/commands/parse`
- `GET /api/asr/providers`
- `POST /api/asr/transcribe`
- `POST /api/tts/synthesize`
- `POST /api/artworks/{artwork_id}/commands`
- `POST /api/artworks/{artwork_id}/undo`
- `POST /api/artworks/{artwork_id}/redo`

延迟指标:

- ASR 响应返回 `metrics.total_ms`、`audio_bytes`、`attempt_count`、`successful_provider` 和 `fallback_count`
- 命令执行响应返回 `metrics.rule_parse_ms`、`llm_planner_ms`、`planner_total_ms`、`execute_ms`、`total_ms`、`llm_attempted`、`llm_succeeded`、`fallback_used` 和 `planner_source`
- 命令延迟指标会同步写入 `voice_command_logs.latency_json`

## 11. 前端交互设计

前端第一屏就是绘图工作台, 包含:

- 顶部作品标题和状态
- 中央 SVG 画布
- 右侧监听状态
- 识别文本
- 最近一次 ASR、规划、执行和端到端延迟指标
- 操作历史

前端没有绘图工具栏、拖拽交互或键盘快捷绘图路径。

## 12. 测试设计

后端测试覆盖:

- 中文数字解析
- 新建画布指令解析
- 圆形颜色和半径解析
- 房子复杂指令拆解
- 撤销同义词
- 创建作品 API
- 语音文本执行为绘图对象
- 撤销和恢复 API
- 批量绘制三颗星星
- 批量改色、整体移动和分步撤销
- 最近对象缩放
- 对象命名、图层元数据和语义标签解析
- 按语义标签批量缩放对象
- 按图层批量移动对象
- ScenePlan schema 骨架
- 路径图形解析和执行
- 复杂语音指令评测集结构和规则用例校验
- planner_expected 复杂场景在规则层进入追问, 不执行半成品计划
- API 执行多主体场景追问时不会写入绘图对象
- ASR Provider 配置发现
- 小米 ASR 请求结构
- 后端 ASR 不可用时返回 503, 前端可继续降级
- 小米 TTS 请求结构和后端接口
- 命令执行响应中的规划、执行和总耗时指标
- 规划器 metrics 对规则解析路径的记录
- 清空画布的确认、取消、执行、撤销、恢复和语音日志状态

前端当前通过 TypeScript 构建验证基础类型和打包流程。

## 13. CI/CD 设计

已实现 GitHub Actions:

- CI: Pull Request、`main` 推送和手动触发时运行后端测试、前端构建和 API smoke test
- CD: `main` 推送、`v*` tag 和手动触发时构建发布包
- Tag 发布: `v*` tag 触发时自动创建 GitHub Release 并上传 zip 产物

发布包包含:

- `backend/app`
- `backend/requirements.txt`
- `frontend/dist`
- `docs`
- `README.md`
- `ROADMAP.md`
- `需求文档.md`
- `设计文档.md`
- `快速启动.bat`

## 14. 当前验证结果

已验证:

- 后端 pytest 已通过, 覆盖解析器、API、SQLite 持久化、撤销恢复、ASR Provider、MiMo 规划器降级、TTS 接口、路径图形、低置信度追问和复杂指令评测集
- 前端 Vite build 已通过, 覆盖 TypeScript 编译和生产打包
- `.env` 加载检查已通过, 已确认能读取本机 `MIMO_API_KEY` 和固定后端地址

未验证:

- 小米 MiMo ASR 和 MiMo-v2.5-Pro 真实账号调用是否可用, 因为当前仅写入本机 `.env`, 未在自动化检查中访问真实接口
- 本地 ASR 模型服务是否可用, 因为本项目只实现适配层, 不下载大模型权重
- 浏览器真实麦克风识别是否可用, 因为该能力依赖浏览器权限、系统麦克风和具体浏览器实现
- 固定端口启动脚本是否能在当前机器直接启动, 因为本次未执行会打开持久终端窗口的 `快速启动.bat`
- PNG 下载是否被浏览器策略阻止, 因为需要真实浏览器交互环境确认下载策略

## 15. 后续建议

1. 扩展命令评测集到 50 到 100 条中文语音指令, 并加入真实 ASR 转写文本版本。
2. 增加 ASR 评测集, 分别记录小米、本地模型和 Web Speech API 的准确率与延迟。
3. 扩展对象命名和多对象选择能力, 支持更多组合筛选和语音确认。
4. 增加 WebSocket 进度推送。
5. 增加 Playwright 浏览器验证, 检查画布渲染和导出路径。
