name: AI Painting CI Smoke

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  smoke:
    name: Workflow smoke
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow registration
        run: echo "AI Painting CI is registered"
