name: AI Painting CI

"on":
  push:
  pull_request:
  workflow_dispatch:

jobs:
  checks:
    name: Backend frontend and Docker checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      AI_PAINTING_DB: /tmp/ai_painting_ci.sqlite3
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.10"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          python -m pip install -r backend/requirements.txt
          npm ci --prefix frontend

      - name: Compile backend
        run: python -m compileall backend/app backend/tests

      - name: Run backend tests
        run: python -m pytest backend/tests -q

      - name: Build frontend
        run: npm run build --prefix frontend

      - name: Validate Docker Compose config
        run: docker compose -f docker-compose.yml config

      - name: Build Docker backup images
        run: docker compose -f docker-compose.yml build

      - name: Start API and verify health
        shell: bash
        run: |
          python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8080 &
          api_pid=$!
          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:8080/health; then
              kill "$api_pid"
              wait "$api_pid" || true
              exit 0
            fi
            sleep 1
          done
          kill "$api_pid" || true
          wait "$api_pid" || true
          exit 1
